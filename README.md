# PolicyAssistant

A retrieval-augmented generation (RAG) assistant that answers employee questions about company policies (HR, expenses, leave, IT, code of conduct). Answers are grounded in documents indexed in Azure AI Search and generated by a chat model deployed in Microsoft Foundry. Every answer cites its sources.

## Solution layout

```
src/
  PolicyAssistant.Api/      Minimal API: /chat, /health
  PolicyAssistant.Web/      Blazor chat UI (interactive server)
  PolicyAssistant.Shared/   DTOs shared by Api and Web
tests/
  PolicyAssistant.Api.Tests/  Unit tests
  PolicyAssistant.Evals/      Evaluation runner
eval/
  golden-questions.json     Evaluation dataset
requests/
  chat.http                 Manual API test requests
```

## Requirements

- .NET 10 SDK. The original plan targeted .NET 9; .NET 10 is used because it is the current LTS release.
- Azure CLI, signed in with `az login`

## Azure prerequisites

Create these resources manually in the Azure portal. This repo does not provision them.

| Resource | Details |
|---|---|
| Foundry project | Chat deployment (e.g. `gpt-4.1-mini`) and embedding deployment (`text-embedding-3-small`) |
| Azure AI Search | Basic tier or higher, semantic ranker enabled |
| Storage account | Blob container named `policies` containing the policy PDFs |
| Search index | Created with the portal's **Import and vectorize data** wizard over the `policies` container, using the embedding deployment. Add a semantic configuration named `default`. |

The wizard's default index field names are `chunk_id` (key), `parent_id`, `chunk` (text), `title` and `text_vector` (vector). All of them can be changed in configuration.

### Role assignments (local development)

Grant the signed-in developer account:

- **Search Index Data Reader** on the Search service
- **Cognitive Services OpenAI User** on the Foundry resource

No API keys are used anywhere. All authentication goes through `DefaultAzureCredential`.

## Configuration

Set endpoints in `src/PolicyAssistant.Api/appsettings.Development.json` or user secrets (endpoints only, never keys):

```json
{
  "AzureSearch": {
    "Endpoint": "https://<search-service>.search.windows.net",
    "IndexName": "<index-name>"
  },
  "AzureOpenAI": {
    "Endpoint": "https://<foundry-resource>.openai.azure.com/"
  }
}
```

## Running

```powershell
dotnet build
dotnet run --project src/PolicyAssistant.Api   # http://localhost:5271
dotnet run --project src/PolicyAssistant.Web   # http://localhost:5248
```

Health check: `GET http://localhost:5271/health`

## Tests

```powershell
dotnet test
```
